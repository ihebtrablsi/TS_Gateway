version: '2'
services:
  finance-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/Finance/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=Finance
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
