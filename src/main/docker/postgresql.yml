version: '2'
services:
  productinventory-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/ProductInventory/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=ProductInventory
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
